# ApnaMock Static Data

This repository stores static JSON data, image assets, and exported question pools for the ApnaMock mock test platform.

## Structure
- /index: global index files
- /tests: fixed test JSON files
- /custom-tests: pools, patterns, presets
- /assets: thumbnails and images
- /meta: version and release logs
